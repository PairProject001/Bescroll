<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Bescroll</title>
    <style>
        body {
            font-family: MS Sans Serif;
            background-color: #6A0DAD; /* Warna ungu */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 75vh;
        }
        form {
            display: flex;
            flex-direction: column;
            max-width: 300px;
            padding: 30px 100px;
            border: 1px solid #ccc;
            border-radius: 5px;
            background-color: #ffffff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }
        h1 {
            text-align: center;
            color: #6A0DAD; /* Warna ungu */
        }
        label {
            font-weight: bold;
            margin-top: 10px;
        }
        input, select {
            padding: 9px 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 3px;
        }
        button {
            background-color: #6A0DAD; /* Warna ungu */
            color: #fff;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-bottom: 10px; /* Jarak antar tombol */
        }
        button:hover {
            background-color: #47097A; /* Warna ungu yang lebih gelap saat hover */
        }
        .register-button {
            text-align: center;
        }
        .separator {
            border-top: 1px solid #ccc;
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div style="margin-top: 120px;">
        <form action="/users/login" method="post" autocomplete="off">
            <h1>Bescroll</h1>
            <% err?.forEach(element => { %>
                <p style="color: red;"><%= element %></p>
            <% }); %>
            <label for="username">User Name</label>
            <input type="text" id="username" name="username">
            <label for="password">Password</label>
            <input type="password" id="password" name="password">
            <button type="submit">Login</button>
        </form>
        <div class="separator"></div>
        <div class="register-button">
            <a href="/users/register"><button>Create New Account</button></a>
        </div>
    </div>
</body>
</html>
