<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Bescroll</title>
    <style>
        body {
            font-family: MS Sans Serif;
            background-color: #6A0DAD; /* Warna ungu */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        form {
            display: flex;
            flex-direction: column;
            max-width: 300px;
            padding: 30px 120px;
            border: 1px solid #ccc;
            border-radius: 5px;
            background-color: #ffffff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }
        h1 {
            text-align: center;
            color: #6A0DAD; /* Warna ungu */
        }
        label {
            font-weight: bold;
            margin-top: 10px;
        }
        input, select {
            padding: 9px 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 3px;
        }
        button {
            background-color: #6A0DAD; /* Warna ungu */
            color: #fff;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background-color: #47097A; /* Warna ungu yang lebih gelap saat hover */
        }
    </style>
</head>
<body>
    <div>
        <form action="/users/register" method="post" autocomplete="off">
            <h1>Bescroll</h1>
            <div class="id">
                <% err?.forEach(el => { %>
                    <h6 style="color: red;"><%= el %></h6>
                <% }); %>
            </div>
            <label for="username">Username</label>
            <input type="text" id="username" name="username">
            <label for="password">Password</label>
            <input type="password" id="password" name="password">
            <label for="fullName">Full Name</label>
            <input type="text" id="fullName" name="fullName">
            <label for="address">Address</label>
            <input type="text" id="address" name="address">
            <label for="birthDate">Birth Date</label>
            <input type="date" id="birthDate" name="birthDate">
            <button type="submit">Register</button>
        </form>
    </div>
</body>
</html>
